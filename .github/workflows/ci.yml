name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint & Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install SDK dependencies
        run: cd sdk && npm install

      - name: Typecheck SDK
        run: cd sdk && npx tsc --noEmit

  verify:
    name: Verify Project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify project structure
        run: |
          echo "Checking project structure..."
          test -f index.html || exit 1
          test -f app.html || exit 1
          test -f styles.css || exit 1
          test -f app.css || exit 1
          test -f main.js || exit 1
          test -f app.js || exit 1
          test -f programs/equxi/src/lib.rs || exit 1
          test -f programs/equxi/src/state.rs || exit 1
          test -f programs/equxi/src/error.rs || exit 1
          test -f sdk/src/index.ts || exit 1
          test -f Anchor.toml || exit 1
          test -f setup.sh || exit 1
          echo "✅ All files present"

      - name: Verify Rust program structure
        run: |
          echo "Checking Rust program..."
          head -5 programs/equxi/src/lib.rs | grep -q "anchor_lang" || exit 1
          grep -q "pub fn handler" programs/equxi/src/instructions/register_agent.rs || exit 1
          grep -q "pub fn handler" programs/equxi/src/instructions/create_bond.rs || exit 1
          grep -q "pub fn handler" programs/equxi/src/instructions/execute_slash.rs || exit 1
          grep -q "pub fn handler" programs/equxi/src/instructions/withdraw_bond.rs || exit 1
          grep -q "pub fn handler" programs/equxi/src/instructions/compensate_victim.rs || exit 1
          grep -q "pub fn handler" programs/equxi/src/instructions/add_constraint.rs || exit 1
          grep -q "pub fn handler" programs/equxi/src/instructions/initialize.rs || exit 1
          echo "✅ All program instructions present"

      - name: Verify SDK
        run: |
          echo "Checking SDK..."
          grep -q "class EquxiClient" sdk/src/index.ts || exit 1
          grep -q "registerAgent" sdk/src/index.ts || exit 1
          grep -q "createBond" sdk/src/index.ts || exit 1
          grep -q "executeSlash" sdk/src/index.ts || exit 1
          echo "✅ SDK complete"

      - name: Verify README
        run: |
          grep -q "Equxi" README.md || exit 1
          grep -q "equxi.sithunyein.com" README.md || exit 1
          echo "✅ README is complete"
